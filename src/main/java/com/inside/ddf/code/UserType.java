package com.inside.ddf.code;

public enum UserType {

	F, // Fasting Glucose High Type(공복 혈당)
    P, // Postprandial Glucose High Type(식후 혈당)
    E, // Excessive Weight Gain Type(체중 과도)
    I  // Insulin Therapy Type (인슐린)
}
